

<p> Welcome, <%= current_user.username %>
<h2> Game Vault </h2>

<% current_user.games.each do |game| %>
  <div>
    
    <a href="/games/<%= game.id %>/showgame"><%= game.gamename %></a>
  </div>
<% end %>